[package]
name = "vtr-kdb"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Independent slang RTL knowledge database and temporal driver tracing for VTR"

[dependencies]
vtr = { path = "../vtr" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
